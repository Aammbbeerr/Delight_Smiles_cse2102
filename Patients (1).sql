Create table Patients
(
	id(7) int NOT NULL auto_increment,
	firstName varchar(15) NOT NULL;
	lastName  varchar(15) NOT NULL;
	dateOfBirth date,
	Sex char(1),
	address varchar(120).
	note varchar(100),	
	PRIMARY KEY (id)
);